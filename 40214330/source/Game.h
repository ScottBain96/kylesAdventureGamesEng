#ifndef GAME_H
#define GAME_H
#include <SFML/Graphics.hpp>

const uint16_t gameWidth = sf::VideoMode().getDesktopMode().width;
const uint16_t gameHeight = sf::VideoMode().getDesktopMode().height;
const int mapHeight = 74;
const int mapWidth = 101;

#endif // GAME_H